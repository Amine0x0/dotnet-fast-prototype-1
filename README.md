# csharp-tcp-sqlite-server

Small learning project.
Raw TCP server in C# that stores messages in SQLite.
First contact with C# and SQL. and whatever nonsense this whole socket stuff is.

## Run
dotnet build
dotnet run or ./bin/... ifykyk

Server listens on port 6767.
Connect using netcat or telnet.
